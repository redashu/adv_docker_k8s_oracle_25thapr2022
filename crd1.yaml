apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  # name must match the spec fields below, and be in the form: <plural>.<group>
  name: sslconfigss.stable.example.com
spec:
  # group name to use for REST API: /apis/<group>/<version>
  group: stable.example.com
  scope: namespaced
  names:
   plural: sslconfigss # used in the URL : /apis/group/version/plural
   singular: sslconfigss # used as alias ON cli for display 
   kind: SslConfigS
   shortnames:
   - sslc
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
      type: object
      properties:
       spec:
        type: object
        properties:
         spec:
          required:  
     

